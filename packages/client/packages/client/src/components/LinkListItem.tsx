import { Box, Text, HStack, Button, Tag, TagLabel, Stack } from "@chakra-ui/react";\nimport { EditIcon, DeleteIcon } from "@chakra-ui/icons";\nimport { Link } from "../types";\n\ninterface LinkListItemProps {\n  link: Link;\n  onUpdate: () => void;\n}\n\nexport default function LinkListItem({ link, onUpdate }: LinkListItemProps) {\n  return (\n    <Box p={4} borderWidth="1px" borderRadius="lg">\n      <Stack spacing={4}>\n        <Box>\n          <Text\n            as="a"\n            href={link.url}\n            target="_blank"\n            rel="noopener noreferrer"\n            fontSize="lg"\n            fontWeight="semibold"\n            color="blue.500"\n            _hover={{ color: "blue.600" }}\n          >\n            {link.title}\n          </Text>\n          {link.description && (\n            <Text color="gray.600" mt={1}>\n              {link.description}\n            </Text>\n          )}\n        </Box>\n        <HStack spacing={2} flexWrap="wrap">\n          {link.tags.map((tag) => (\n            <Tag key={tag.id} size="md" colorScheme="blue" variant="subtle">\n              <TagLabel>{tag.name}</TagLabel>\n            </Tag>\n          ))}\n        </HStack>\n        <HStack spacing={2}>\n          <Button\n            size="sm"\n            colorScheme="blue"\n            variant="outline"\n            leftIcon={<EditIcon />}\n          >\n            Edit\n          </Button>\n          <Button\n            size="sm"\n            colorScheme="red"\n            variant="outline"\n            leftIcon={<DeleteIcon />}\n          >\n            Delete\n          </Button>\n        </HStack>\n      </Stack>\n    </Box>\n  );\n}
